name: KanjiTest Build & Deploy Engine
 
# Triggers: Runs when you update data or the generator script
on:
  push:
    branches: [ main ]
    paths:
      - 'data/**'
      - 'generator.py'
      - '.github/workflows/kanji-main.yml' 
  workflow_dispatch:
    inputs:
      reason:
        description: 'Manual build trigger'
        required: false
        default: 'Updating Kanji Collection'

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 🛰️ Checkout Codebase
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Install Requirements
        run: |
          python -m pip install --upgrade pip
          pip install Jinja2 MarkupSafe

      - name: 🏗️ Execute Universal Kanji Generator
        run: |
          # Ensure generator.py is in your repository root
          # This script reads from data/[level]/kanji/data.json
          python generator.py

      - name: 🔍 SEO Validation
        run: |
          if [ -f sitemap.xml ]; then
            echo "✅ Sitemap generated in root."
          else
            echo "❌ Sitemap generation failed. Check generator.py logic."
            exit 1
          fi

      - name: 🚀 Deploy Unique SEO Pages to Root
        if: github.event_name != 'pull_request'
        run: |
          git config --global user.name "kanjitest-automation-bot"
          git config --global user.email "bot@kanjitest.online"
          
          # Force add level folders to ensure clean URLs in root
          # This supports unlimited pages for test.singhyogendra.com.np
          git add n5/ n4/ n3/ n2/ n1/ sitemap.xml
          
          # Commit with [skip ci] to avoid recursive action triggers
          git commit -m "chore: auto-generate unlimited SEO kanji pages [skip ci]" || echo "No changes to commit"
          git push origin main
